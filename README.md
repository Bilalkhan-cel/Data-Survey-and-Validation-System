# Data-Survey-and-Validation-System
This following project is based upon collecting the data from the user and system auto validates the data being collected . It is built to be fully dynamic, allowing any survey to be executed within the system without requiring any modifications to the underlying code
